# UI Refresh Source Assets

These are unprocessed visual sources supplied for the next UI pass. They are
reference material only and are not used by the game at runtime.

- `clouds/`: cloud transition source images
- `modals/`: tutorial and settings source images

Process and copy approved assets into `assets/ui/` before wiring them into the
game.
