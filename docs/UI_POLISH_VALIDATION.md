# UI 改造验证

## 游戏页与原图（2026-09-07）

正常窗口实际点击三模式“查看原图”与“继续拼图”；完整图片使用 KEEP_ASPECT_CENTERED，模态内输入不作用于棋盘，关闭后状态快照相同。390×844 手机比例及桌面短窗口检查通过：按钮不与标题／棋盘重叠，图片和关闭按钮均在视口内。复用现有关卡原图，无新增生成素材。游戏日志仅有已知 macOS orientation 警告。测试套件按项目要求未运行，手机尺寸检查不等同真机触控验收。

## 模式入口与完成页

模式入口显示多边形／凹凸／交换名称，切换时同步展示一句玩法说明；三项实际点击、说明变化及开始进入交换模式通过。完成页按纹理原始比例计算画框，完整图像不裁切，卡片不碰撞庆祝区。三模式使用现有 debug_force_complete 触发真实完成回调，验证返回按钮和再次进入，均 `ok: true`；诊断最初只等待 0.65 秒而早于完整返回转场，等待全流程后通过。所有临时完成进度在停止游戏后恢复为检查前文件。展示截图已人工检查，日志无脚本错误；套件未运行。
